services:
  keycloak-bootstrap:
    image: timescale/timescaledb@sha256:0af03ecf697825f6ddae76fd275d16bf46007bed6d00eb3d754779cb7db96fa6
    container_name: keycloak-bootstrap
    restart: "no"
    networks:
      - postgres
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_ADMIN_USER}
      POSTGRES_PASSWORD: ${POSTGRES_ADMIN_PASSWORD}
      POSTGRES_DB: postgres
      POSTGRES_KEYCLOAK_PASSWORD: ${POSTGRES_KEYCLOAK_PASSWORD:-}
    volumes:
      - ./configs/postgres/ensure-keycloak-db.sh:/ensure-keycloak-db.sh:ro
    entrypoint: ["/bin/bash", "/ensure-keycloak-db.sh"]

  keycloak:
    image: webservices/keycloak:local-build
    build:
      context: .
      dockerfile: ./stack.containers/keycloak/Dockerfile
    container_name: keycloak
    restart: unless-stopped
    command:
      - start
      - --import-realm
    networks:
      caddy:
      postgres:
    depends_on:
      keycloak-bootstrap:
        condition: service_completed_successfully
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN_USER:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-}
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${POSTGRES_KEYCLOAK_PASSWORD:-}
      KC_HEALTH_ENABLED: "true"
      KC_HTTP_ENABLED: "true"
      KC_METRICS_ENABLED: "true"
      KC_HOSTNAME: https://keycloak.${DOMAIN}
      KC_PROXY_HEADERS: xforwarded
      JAVA_OPTS_KC_HEAP: "-XX:InitialRAMPercentage=35 -XX:MaxRAMPercentage=60"
    volumes:
      - ./configs/keycloak/realm:/opt/keycloak/data/import:ro
    deploy:
      resources:
        limits:
          memory: ${KEYCLOAK_MEMORY_LIMIT:-2G}
          cpus: '${KEYCLOAK_CPU_LIMIT:-2.0}'
    healthcheck:
      test:
        - CMD-SHELL
        - |
          bash -ec "exec 3<>/dev/tcp/127.0.0.1/9000 && \
          printf 'GET /health/ready HTTP/1.0\r\nHost: 127.0.0.1\r\n\r\n' >&3 && \
          grep -q ' 200 ' <&3"
      interval: 10s
      timeout: 5s
      retries: 60
      start_period: 420s
